{
  "name": "cdt-performance-test",
  "version": "1.0.0",
  "description": "CDT (Compression Dictionary Transport) performance test automation",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "serve": "npx http-server -p 8080 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "http-server": "^14.1.1",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
